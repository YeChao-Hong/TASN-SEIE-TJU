验证（运行validate.py）之前需要改掉validate.py中的pretrained model路径和验证集路径
训练（运行train.py）之前需要改到base_options.py和train_options.py里的相关参数
datasets at:
https://drive.google.com/file/d/1fEiUo22GBSnWD7nfEwDW86Eiza-pOEJm/view

运行preprocess.py将视频处理为图像后作为模型输入
